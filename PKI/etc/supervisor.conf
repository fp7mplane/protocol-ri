# TLS server certificate request

# This file is used by the openssl req command. The subjectAltName cannot be
# prompted for and must be specified in the SAN environment variable.

[ default ]
SAN                     = DNS:monroe-supervisor.polito.it

[ req ]
default_bits            = 2048                  # RSA key size
encrypt_key             = yes                   # Protect private key
default_md              = sha1                  # MD to use
utf8                    = yes                   # Input is UTF-8
string_mask             = utf8only              # Emit UTF-8 strings
prompt                  = no                   # Prompt for DN
distinguished_name      = supervisor_dn	        # DN template
req_extensions          = supervisor_reqext	# Desired extensions

[ supervisor_dn ]
0.domainComponent       = "org"
1.domainComponent       = "monroe"
organizationName        = "POLITO"
organizationalUnitName  =  "Supervisors"
commonName              =  "Monroe_Supervisor_POLITO_1"

[ supervisor_reqext ]
keyUsage                = critical,digitalSignature,keyEncipherment
extendedKeyUsage        = serverAuth,clientAuth
subjectKeyIdentifier    = hash
subjectAltName 		= $ENV::SAN
